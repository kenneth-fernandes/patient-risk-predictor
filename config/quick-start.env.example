# Quick Start Configuration Template
# This is a simplified setup for quick testing - for better organization, use specific config files:
#
# For production: copy production.env.example to production.env
# For secrets: copy secrets.env.example to secrets.env  
# For development: use development.env (already configured)
# For Docker: use docker.env (already configured)

# Quick setup (Database Configuration)
# IMPORTANT: Change this password before using in production!
POSTGRES_DB=mlflow
POSTGRES_USER=mlflow
POSTGRES_PASSWORD=change_this_password_now

# Basic Configuration
LOG_LEVEL=INFO
ENVIRONMENT=development
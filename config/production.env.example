# Production Environment Configuration Template
# Copy this file to production.env and update the values as needed
# Note: Database credentials are configured in secrets.env.example

# Server Configuration
HOST=0.0.0.0
PORT=8000
ENVIRONMENT=production
WORKERS=4

# Logging Configuration
LOG_LEVEL=INFO
ENABLE_STRUCTURED_LOGGING=true
LOG_TO_FILE=true
LOG_DIR=/app/logs

# MLflow Configuration
MLFLOW_TRACKING_URI=http://mlflow:5000